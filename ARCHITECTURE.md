# 🏗️ Arquitectura del Sistema EnfoadsIA

## Diagrama de Componentes

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Home       │  │  AdCreator   │  │  FaceSwap    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ BrandVault   │  │  Settings    │  │  Landing     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              API Client (services/api.ts)                 │  │
│  │  - Dynamic URL from localStorage                          │  │
│  │  - All endpoints typed with TypeScript                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      NGROK TUNNEL                                │
│  - Public URL: https://xxxxx.ngrok-free.app                     │
│  - Forwards to localhost:5000                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (Flask + SocketIO)                    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐│
│  │                    API Endpoints                            ││
│  │  GET  /              - Health check                         ││
│  │  GET  /gpu-status    - VRAM monitoring                      ││
│  │  POST /magic-prompt  - Prompt enhancement                   ││
│  │  POST /generate-image - SDXL Lightning                      ││
│  │  POST /face-swap     - InsightFace swap                     ││
│  │  POST /render-video  - Avatar + voice                       ││
│  │  POST /render-multi-scene - Multi-scene videos              ││
│  │  GET  /avatars       - Avatar list                          ││
│  │  GET/POST /api/assets - Asset management                    ││
│  │  GET  /api/jobs/:id  - Job status                           ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐│
│  │                  VRAM Manager                               ││
│  │  - Auto offloading to CPU                                   ││
│  │  - Only 1 model in GPU at a time                            ││
│  │  - torch.cuda.empty_cache() after each task                 ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐│
│  │                  Job Queue System                           ││
│  │  - Threading-based queue                                    ││
│  │  - SocketIO for real-time updates                           ││
│  │  - Status tracking: queued → processing → completed         ││
│  └────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      AI MODELS (GPU/CPU)                         │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │  SDXL Lightning  │  │  Faster-Whisper  │                    │
│  │  - 4 steps       │  │  - Base model    │                    │
│  │  - ~6GB VRAM     │  │  - ~1GB VRAM     │                    │
│  │  - 2-3s/image    │  │  - 10x realtime  │                    │
│  └──────────────────┘  └──────────────────┘                    │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │   InsightFace    │  │  LivePortrait    │                    │
│  │  - buffalo_l     │  │  - (Planned)     │                    │
│  │  - ~2GB VRAM     │  │  - ~4GB VRAM     │                    │
│  │  - 1s/swap       │  │  - Face anim     │                    │
│  └──────────────────┘  └──────────────────┘                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    GOOGLE COLAB T4 GPU                           │
│  - 15GB VRAM                                                     │
│  - CUDA 11.8+                                                    │
│  - Free tier with limits                                        │
│  - ~12 hour sessions                                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## Flujo de Datos

### 1. Generación de Imagen

```
User Input (Frontend)
    │
    ├─→ [Optional] Magic Prompt Enhancement
    │       │
    │       └─→ Enhanced Prompt
    │
    └─→ POST /generate-image
            │
            ├─→ Load SDXL to GPU (offload others)
            │
            ├─→ Generate Image (4 steps, ~2-3s)
            │
            ├─→ Convert to Base64
            │
            └─→ Return to Frontend
                    │
                    └─→ Display + Save to Assets
```

### 2. Face Swap

```
User Uploads 2 Images (Frontend)
    │
    └─→ POST /face-swap
            │
            ├─→ Load InsightFace to GPU (offload others)
            │
            ├─→ Detect Faces in Both Images
            │
            ├─→ Perform Swap (~1s)
            │
            ├─→ Convert Result to Base64
            │
            └─→ Return to Frontend
                    │
                    └─→ Display + Save to Assets
```

### 3. Video Generation (Queued)

```
User Input (Script + Avatar)
    │
    └─→ POST /render-video
            │
            ├─→ Create Job ID
            │
            ├─→ Add to Queue
            │
            └─→ Return Job ID
                    │
                    ├─→ Frontend polls /api/jobs/:id
                    │
                    └─→ SocketIO emits progress updates
                            │
                            ├─→ 10%  - Queued
                            ├─→ 30%  - Animating face
                            ├─→ 70%  - Generating subtitles
                            └─→ 100% - Completed
                                    │
                                    └─→ Return Video URL
```

---

## Gestión de VRAM

### Estrategia de Offloading

```
┌─────────────────────────────────────────────────────────────┐
│                    T4 GPU (15GB VRAM)                        │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Active Model (GPU)                                     │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  SDXL Lightning (~6GB)                            │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Buffer (~2GB)                                          │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Free (~7GB)                                            │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    RAM (Offloaded Models)                    │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Whisper    │  │ InsightFace  │  │ LivePortrait │      │
│  │   (CPU)      │  │   (CPU)      │  │   (CPU)      │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### Proceso de Cambio de Modelo

```python
# Cuando se necesita usar InsightFace:

1. offload_models(except_model='insightface')
   ├─→ SDXL.to('cpu')
   ├─→ Whisper.to('cpu')
   └─→ torch.cuda.empty_cache()

2. insightface.to('cuda')

3. Perform face swap

4. [Optional] offload_models() para liberar todo
```

---

## Persistencia de Datos

```
backend/data/
├── assets.json          # Biblioteca de assets generados
│   └── [
│         {
│           "id": "img_123",
│           "type": "image",
│           "url": "data:image/png;base64,...",
│           "prompt": "...",
│           "created_at": "2025-12-27T..."
│         }
│       ]
│
├── cache/               # Caché de imágenes (futuro)
│   └── <hash>.png
│
└── app.log             # Logs del servidor
```

---

## Seguridad y Límites

### Implementado
- ✅ CORS configurado para desarrollo
- ✅ Validación básica de inputs
- ✅ Error handlers globales
- ✅ Timeouts en generaciones

### Recomendado para Producción
- ⚠️ Rate limiting (requests/minute)
- ⚠️ Autenticación JWT
- ⚠️ Sanitización de prompts
- ⚠️ Límites de tamaño de archivo
- ⚠️ HTTPS obligatorio
- ⚠️ API keys para acceso

---

## Escalabilidad

### Limitaciones Actuales (Colab Free)
- 1 usuario concurrente (cola secuencial)
- ~12 horas de sesión máxima
- Almacenamiento temporal
- Sin persistencia entre sesiones

### Mejoras Futuras
- **Colab Pro**: Sesiones más largas, mejor GPU
- **Cloud Deployment**: AWS/GCP con GPU
- **Redis Queue**: Para múltiples workers
- **S3/Cloud Storage**: Persistencia de assets
- **Load Balancer**: Múltiples instancias

---

## Monitoreo y Debugging

### Logs Disponibles
```python
# Backend logs
[INFO] Loading SDXL Lightning to RAM...
[*] Offloading whisper to CPU...
[+] Job vid_123 Finished.
[!] Face Swap Error: No face detected
```

### Endpoints de Diagnóstico
- `GET /` - Health check
- `GET /gpu-status` - VRAM usage
- `GET /api/jobs/:id` - Job status

### Herramientas
- Google Colab logs (stdout)
- Browser DevTools (Network tab)
- SocketIO events (real-time)

---

## Tecnologías Utilizadas

### Frontend
- **React 19** - UI framework
- **TypeScript** - Type safety
- **Vite** - Build tool
- **React Router** - Navigation
- **Zustand** - State management
- **Socket.IO Client** - Real-time

### Backend
- **Flask** - Web framework
- **Flask-SocketIO** - WebSockets
- **PyTorch** - Deep learning
- **Diffusers** - Stable Diffusion
- **Transformers** - Hugging Face
- **InsightFace** - Face analysis
- **Faster-Whisper** - Speech-to-text
- **MoviePy** - Video editing
- **Pyngrok** - Public tunneling

### Infrastructure
- **Google Colab** - GPU hosting
- **Ngrok** - Public access
- **Git/GitHub** - Version control

---

## Próximas Mejoras Arquitecturales

### Corto Plazo
1. Sistema de caché de imágenes
2. Configuración desde archivo JSON
3. Logging estructurado
4. Health checks automáticos

### Mediano Plazo
1. Redis para queue distribuida
2. Celery para workers
3. PostgreSQL para persistencia
4. Docker containers

### Largo Plazo
1. Kubernetes deployment
2. Auto-scaling
3. Multi-region
4. CDN para assets

---

**Documentado por**: Antigravity AI  
**Fecha**: 2025-12-27  
**Versión**: 2.0
